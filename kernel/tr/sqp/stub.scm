
; File:  stub.scm
; Copyright (C) 2004 The Institute for System Programming of the Russian Academy of Sciences (ISP RAS)

(define declare (lambda x '()))
(define unit (lambda x '()))
(define uses (lambda x '()))


(define lr2por "lr2por")
(define lr2por-lib "lr2por-lib")
(define xquery-lr "xquery-lr")
(define common-lib "common-lib")
(define srfi-1 "srfi-1")
(define impl-differences "impl-differences")
(define mark-lreturns "mark-lreturns")
(define extras "extras")
(define foreign-declare "foreign-declare")
(define popt "popt")
(define dschema "dschema")
(define dschema-utils "dschema-utils")
(define scm-error-codes "scm-error-codes")